name: bank-marketing-ci

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: string, default: "namadataset_preprocessing"}
    command: "python modelling.py --data_path {data_path}"
